"""
It would probably be best to use a logging library here.
Python's built in logging or python-json-logger (Because JSON would be easier to
read if function to read and filter logs would be implemented).
"""
